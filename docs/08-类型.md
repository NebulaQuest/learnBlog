## 类型

### JavaScript的数据类型

- 空值（null）
- 未定义（undefined）
- 数字（number）
- 字符串（string）
  - 对象（Object）包含对象，数组，函数，后两者是object的子类型
- 布尔值（boolean）
- 符号（Symbol）

### typeof

- 可以使用typeof来实现对JavaScript中的变量进行类型判断
- 返回值是类型的字符串形式，如‘string’

##### javaScript中是没有变量的类型，只有值的类型，当前变量存放的是数字，那么就是数字类型，如果存放的是字符串，那么就是字符类型

### undefined

- undefined 表示已经对这个变量进行了声明但是还没有赋值
- 在作用域中没有声明的变量是  undeclared

### 数组

- 使用 delete 运算符可以将单元从数组中删除，但是请注意，单元删除后，数组的 length 属性并不会发生变化。第 5 章将详细介绍 delete 运算符。
- 注意类数组和数组的区别 [介绍](./04-介绍arguments参数.md)

### 数字

- 默认情况下大部分数字都以十进制显示，小数部分最后面的 0 被省略
- 使用 `a.toFixed( 4 )` 或者`toPrecision(..)` 可以对小数位进行约束
- . 运算符需要给予特别注意，因为它是一个有效的数字字符，会被优先识别为数字常量的一部分

```javascript
    // 无效语法：
    42.toFixed( 3 ); // SyntaxError
    // 下面的语法都有效：
    (42).toFixed( 3 ); // "42.000"
    0.42.toFixed( 3 ); // "0.420"
    42..toFixed( 3 ); // "42.000"
```

- 0.1 + 0.2 === 0.3; // false 简单来说，二进制浮点数中的 0.1 和 0.2 并不是十分精确，它们相加的结果并非刚好等于0.3，而是一个比较接近的数字 0.30000000000000004，所以条件判断结果为 false。
- 整数的最大值 2^53 - 1 最小值是 -9007199254740991

### 其他

- null 指空值（empty value）
- undefined 指没有值（missing value）
- NaN 是一个特殊值，它和自身不相等，是唯一一个非自反（自反，reflexive，即 x === x 不成立）的值。
- 判断NaN 用 `Number.isNaN( b );`
- JS中存在+0 和 -0 两个值加法和减法运算不会得到负零负零在开发调试控制台中通常显示为 -0，但在一些老版本的浏览器中仍然会显示为 0。根据规范，对负零进行字符串化会返回 "0"

### 引用

JavaScript 引用指向的是值。如果一个值有 10 个引用，这些引用指向的都是同一个值，它
们相互之间没有引用 / 指向关系。

- 修改引用的值的时候注意是否要修改原数组

### 原生函数

常用的原生函数有：
- String()
- Number()
- Boolean()
- Array()
- Object()
- Function()
- RegExp()
- Date()
- Error()
- Symbol()——ES6 中新加入的！

详细信息查看 [对象章节中内置对象对于变量的解释](./05-对象.md)

- 通过一般通过 Object.prototype.toString(..) 来查看变量的内部属性

```javascript
Object.prototype.toString.call( [1,2,3] );
// "[object Array]"
Object.prototype.toString.call( /regex-literal/i );
// "[object RegExp]"
```
### 值的强制转换
